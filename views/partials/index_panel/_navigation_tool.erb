<nav>
  <ul>
    <li><a href="#">SELECTION</a></li>
    <li><a href="#">ALL</a></li>
    <li class="dropdown_nav">
      <a href="#">GENRE</a>
      <div class="dropdown-content_nav">
        <!-- Populate with genres dynamically -->
        <% Asset.distinct.pluck(:genre).each do |genre| %>
          <a href="/genre/<%= genre %>"><%= genre %></a>
        <% end %>
      </div>
    </li>
    <li class="dropdown_nav">
      <a href="#">DIRECTOR</a>
      <div class="dropdown-content_nav">
        <!-- Populate with directors dynamically -->
        <% Asset.distinct.pluck(:directors).each do |director| %>
          <a href="/director/<%= director %>"><%= director %></a>
        <% end %>
      </div>
    </li>
    <li class="dropdown_nav">
      <a href="#">COUNTRY</a>
      <div class="dropdown-content_nav">
        <!-- Populate with countries dynamically -->
        <% Asset.distinct.pluck(:country).each do |country| %>
          <a href="/country/<%= country %>"><%= country %></a>
        <% end %>
      </div>
    </li>
  </ul>
</nav>
