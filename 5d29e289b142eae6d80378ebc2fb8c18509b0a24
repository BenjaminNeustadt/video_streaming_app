This all needs to be tested using Ngrok...

This is preliminary info that we can use to guage where a user is connecting from...
The data we will store is, where user first logged in from...
Where user last connected from (so data at start if user's account and data at last state)
We can use this data to switch the language for the user if we like...
